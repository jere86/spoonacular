{
  "version": 2,
  "builds": [{ "src": "index.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/users(.*)", "dest": "/cors-proxy.js" },
    { "src": "/images(.*)", "dest": "/cors-proxy.js" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
