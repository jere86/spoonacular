{
  "version": 2,
  "builds": [{ "src": "index.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/users(.*)", "dest": "/users" },
    { "src": "/images(.*)", "dest": "/images" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
